(function(){

  var root = this;

  var CloudService = root.CloudService = {
    per_page : 20,
    page: 1,
  };


  CloudService.getArtists = function getArtists(params, callbackFunc, page, per_page) {
	
  }


  CloudService.getTracks = function getTracks(params, callbackFunc, page, per_page) {
	  
  }
  
  CloudService.getTags = function getTracks(params, callbackFunc) {
	 
  }
}).call(this);